% vim: spl=pt
\begin{exercício}{Espectro de um operador idempotente}{ex24}
  Seja \(T \in \bounded(X)\) um operador idempotente, isto é, \(T^2 = T.\) Mostre que \(\sigma(T) \subset \set{0,1}\) e \(\sigma(T) = \set{0,1}\) no caso \(T \notin \set{0,\unity}.\)
\end{exercício}
\begin{proof}[Resolução]

\end{proof}
